---
phase: 09-public-registration
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - app/registration/[compId]/success/page.tsx
autonomous: true
requirements: [REG-06, REG-07, REG-08]

must_haves:
  truths:
    - "Success page shows 'Registration Confirmed' heading, athlete full name, and competition name"
    - "Success page displays a table of assigned serials per event with event name, bell weight, and serial number"
    - "Serial values are displayed in bright-bronze monospace font"
    - "Success page shows screenshot instruction text"
    - "'Register another athlete' button navigates back to the registration form"
    - "Page is a server component that fetches data by registrantId query param — refreshable and bookmarkable"
    - "Multi-event registration shows multiple rows in the serial table"
  artifacts:
    - path: "app/registration/[compId]/success/page.tsx"
      provides: "Registration success/receipt page"
      min_lines: 60
  key_links:
    - from: "app/registration/[compId]/success/page.tsx"
      to: "lib/actions/registrations.ts"
      via: "getRegistrationData function call"
      pattern: "import.*getRegistrationData.*from.*registrations"
---

<objective>
Build the registration success page at `/registration/[compId]/success` that displays the athlete's assigned serial numbers in a receipt-style card, with a "Register another athlete" button for coaches registering multiple athletes.

Purpose: Provides the confirmation receipt that athletes screenshot or bookmark. Must work as a standalone bookmarkable URL per D-07/D-08.
Output: Server component success page with serial number table.
</objective>

<execution_context>
@/Users/clarence/kova-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clarence/kova-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-public-registration/09-CONTEXT.md
@.planning/phases/09-public-registration/09-UI-SPEC.md
@.planning/phases/09-public-registration/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From lib/actions/registrations.ts:
```typescript
export async function getRegistrationData(registrantId: string): Promise<{
  registrant: Registrant
  entries: RegistrationEntry[]
  competition: Competition
} | null>
```

From lib/schema.ts:
```typescript
export type Registrant = {
  id: string
  competitionId: string
  lastName: string
  firstName: string
  gender: string
  bodyWeightKg: number
  country: string
  club: string | null
  coach: string | null
  createdAt: Date
}

export type RegistrationEntry = {
  id: string
  registrantId: string
  competitionId: string
  event: string    // 'LC' | 'JERK' | 'SNATCH'
  bellWeight: string  // e.g. '2x16'
  duration: number  // 10 or 5
  serial: string    // e.g. 'GPC-0001'
  status: string
  createdAt: Date
}

export type Competition = {
  id: string
  name: string
  date: string
  // ... other fields
}
```

Styling tokens from UI-SPEC:
- Page wrapper: `min-h-screen bg-background px-8 py-12`
- Content: `mx-auto max-w-[640px]`
- Card: `rounded-xl bg-card p-8 ring-1 ring-foreground/10`
- Heading: `text-2xl font-semibold text-parchment`
- Subtext: `text-sm text-raw-steel`
- Serial values: `text-bright-bronze font-mono`
- Button: `w-full rounded-2xl bg-patina-bronze py-3 font-bold text-parchment transition-colors hover:bg-bright-bronze`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build registration success page</name>
  <files>app/registration/[compId]/success/page.tsx</files>
  <read_first>
    - lib/actions/registrations.ts (getRegistrationData function — return shape)
    - lib/schema.ts (Registrant, RegistrationEntry, Competition types)
    - app/organizerdb/page.tsx (server component data fetching pattern)
    - .planning/phases/09-public-registration/09-UI-SPEC.md (success page layout contract lines 147-159, copywriting contract)
    - .planning/phases/09-public-registration/09-CONTEXT.md (D-07 data flow, D-08 no session scoping)
  </read_first>
  <action>
Create `app/registration/[compId]/success/page.tsx` as a **server component** (no 'use client' directive).

```typescript
import { getRegistrationData } from '@/lib/actions/registrations'
import { notFound } from 'next/navigation'
import Link from 'next/link'
```

**Page function:**
```typescript
export default async function RegistrationSuccessPage({
  params,
  searchParams,
}: {
  params: Promise<{ compId: string }>
  searchParams: Promise<{ registrantId?: string }>
}) {
  const { compId } = await params
  const { registrantId } = await searchParams

  if (!registrantId) {
    notFound()
  }

  const data = await getRegistrationData(registrantId)

  if (!data) {
    notFound()
  }

  const { registrant, entries, competition } = data
  const fullName = `${registrant.firstName} ${registrant.lastName}`
```

**Layout (per UI-SPEC success page contract):**
```tsx
<div className="min-h-screen bg-background px-8 py-12">
  <div className="mx-auto max-w-[640px]">

    {/* Page heading */}
    <h1 className="text-2xl font-semibold text-parchment">Registration Confirmed</h1>
    <p className="mt-1 text-sm text-raw-steel">
      {fullName} &mdash; {competition.name}
    </p>

    {/* Serial numbers card */}
    <div className="mt-6 rounded-xl bg-card p-8 ring-1 ring-foreground/10">
      <h2 className="text-base font-semibold text-parchment mb-4">Your Serial Numbers</h2>

      {/* Table */}
      <table className="w-full text-sm">
        <thead>
          <tr className="border-b border-raw-steel/20">
            <th className="pb-2 text-left font-semibold text-raw-steel">Event</th>
            <th className="pb-2 text-left font-semibold text-raw-steel">Bell Weight</th>
            <th className="pb-2 text-left font-semibold text-raw-steel">Serial</th>
          </tr>
        </thead>
        <tbody>
          {entries.map((entry) => (
            <tr key={entry.id} className="border-b border-raw-steel/10">
              <td className="py-3 text-parchment">
                {entry.event === 'LC' ? 'Long Cycle' : entry.event === 'JERK' ? 'Jerk' : 'Snatch'}
              </td>
              <td className="py-3 text-parchment">{entry.bellWeight}</td>
              <td className="py-3 text-bright-bronze font-mono">{entry.serial}</td>
            </tr>
          ))}
        </tbody>
      </table>

      {/* Screenshot instruction */}
      <p className="mt-4 text-sm text-raw-steel">
        Screenshot or save your serial numbers. These are your competition identifiers.
      </p>
    </div>

    {/* Register another button */}
    <Link
      href={`/registration/${compId}`}
      className="mt-6 block w-full rounded-2xl bg-patina-bronze py-3 text-center font-bold text-parchment transition-colors hover:bg-bright-bronze active:opacity-80"
    >
      Register another athlete
    </Link>

  </div>
</div>
```

**Key details:**
- Event display names: 'LC' -> 'Long Cycle', 'JERK' -> 'Jerk', 'SNATCH' -> 'Snatch'
- Serial values in `text-bright-bronze font-mono` per UI-SPEC
- Table with 3 columns: Event, Bell Weight, Serial
- "Register another athlete" is a Link (not button) back to `/registration/[compId]` (REG-08)
- The page is fully server-rendered — no client JS needed (D-07: refreshable, bookmarkable)
- No session scoping (D-08) — works for any valid registrantId
- If registrantId is missing or data not found, call `notFound()`
  </action>
  <verify>
    <automated>grep -n "getRegistrationData\|Registration Confirmed\|Register another\|font-mono\|text-bright-bronze\|Screenshot\|serial" app/registration/\[compId\]/success/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/registration/[compId]/success/page.tsx exists with NO 'use client' directive (server component)
    - Imports getRegistrationData from '@/lib/actions/registrations'
    - Awaits both `params` and `searchParams` (Next.js 16 async pattern)
    - Calls notFound() when registrantId missing or getRegistrationData returns null
    - Contains h1 "Registration Confirmed"
    - Displays `{firstName} {lastName}` and `{competition.name}` in subline
    - Renders table with Event, Bell Weight, Serial columns
    - Serial values have className containing "text-bright-bronze" and "font-mono"
    - Contains text "Screenshot or save your serial numbers. These are your competition identifiers."
    - Contains Link to `/registration/${compId}` with text "Register another athlete"
    - Event display names: LC -> "Long Cycle", JERK -> "Jerk", SNATCH -> "Snatch"
    - Multi-event entries render multiple table rows (entries.map)
  </acceptance_criteria>
  <done>Success page displays registration receipt with serial table, screenshot instruction, and "Register another athlete" link — fully server-rendered, bookmarkable, no session scoping</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors
2. Register an athlete via the form (from Plan 02), verify redirect to success page
3. Refresh the success page — data persists (server component, bookmarkable)
4. Click "Register another athlete" — returns to blank form
5. Multi-event registration shows multiple rows with different serials
</verification>

<success_criteria>
- Success page renders for valid registrantId with correct competition name, athlete name, serial table
- Serial numbers displayed in bright-bronze monospace
- Screenshot instruction text present
- "Register another athlete" navigates back to form
- 404 shown for invalid registrantId
- Page is refreshable (server component, no client state dependency)
</success_criteria>

<output>
After completion, create `.planning/phases/09-public-registration/09-03-SUMMARY.md`
</output>
