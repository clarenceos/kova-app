# Quick Task 260327-24f: Summary

**Task:** Remove iOS recording block from app/record/page.tsx
**Status:** Complete
**Commit:** 35a7738

## What Changed

**app/record/page.tsx** — Removed the iOS user-agent detection block (`iPad|iPhone|iPod` regex + `MacIntel` + `maxTouchPoints > 1` check) that prevented iOS users from accessing the recorder. The `supported` check now only gates on `captureStream` availability. Updated the unsupported browser message to be platform-neutral.

Incremental IndexedDB chunk writes (`start(10_000)`, `appendChunk`, `assembleChunks`, `clearChunks`) remain untouched as the crash safety net for all platforms.
