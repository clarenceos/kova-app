# Quick Task: Remove flash verdict indicator from entry detail

## What Changed
- Removed `flashVisible` state, `lastTap` variable, flash useEffect, and flash DOM element
- Right column now starts immediately with rep count header + scrollable pills
- Time-sync ring highlight on current pill remains intact

## Files Modified
- `components/profile/EntryDetailClient.tsx`
