---
phase: quick
plan: 260405-0uq
type: execute
wave: 1
depends_on: []
files_modified:
  - app/organizerdb/queue/_components/TimetableGrid.tsx
  - app/organizerdb/_components/AddRegistrantDialog.tsx
  - app/organizerdb/_components/DashboardClient.tsx
  - app/organizerdb/_components/RegistrationsTable.tsx
autonomous: true
requirements: [quick-260405-0uq]
must_haves:
  truths:
    - "Organizer can delete a block from the queue timetable"
    - "Organizer can add a registrant directly from the dashboard without navigating away"
    - "Adding a registrant creates the registrant and entries with serials in the database"
    - "After deleting a block, remaining blocks renumber and times recalculate"
  artifacts:
    - path: "app/organizerdb/queue/_components/TimetableGrid.tsx"
      provides: "Delete block handler and UI button"
    - path: "app/organizerdb/_components/AddRegistrantDialog.tsx"
      provides: "Add registrant dialog with event/bell/duration selection"
    - path: "app/organizerdb/_components/DashboardClient.tsx"
      provides: "Add Entry button wired in action bar"
  key_links:
    - from: "TimetableGrid.tsx handleDeleteBlock"
      to: "recalculate()"
      via: "splice + renumber + recalculate"
    - from: "AddRegistrantDialog"
      to: "registerAthlete server action"
      via: "import from lib/actions/registrations"
---

<objective>
Add two organizer workflow improvements: (1) delete block from queue timetable, (2) add registrant directly from the dashboard registration list.

Purpose: Organizers need to remove unwanted/empty blocks from the timetable and add athletes directly without navigating to the public registration page.
Output: Updated TimetableGrid with delete-block capability, new AddRegistrantDialog component, updated DashboardClient with Add Entry button.
</objective>

<execution_context>
@/Users/clarence/kova-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clarence/kova-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/organizerdb/queue/_components/TimetableGrid.tsx
@app/organizerdb/queue/_components/InsertBlockRow.tsx
@app/organizerdb/_components/DashboardClient.tsx
@app/organizerdb/_components/EditRegistrantDialog.tsx
@app/organizerdb/_components/RegistrationsTable.tsx
@lib/actions/registrations.ts
@lib/actions/dashboard.ts
@lib/queue/types.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/queue/types.ts:
```typescript
export interface TimeBlock {
  blockNumber: number;
  startTime: number;
  endTime: number;
  platforms: (PlatformSlot | null)[];
}
export type Conflict = RestConflict | CoachConflict | JudgeConflict;
```

From lib/actions/registrations.ts:
```typescript
export async function registerAthlete(input: {
  competitionId: string
  lastName: string
  firstName: string
  gender: 'Male' | 'Female'
  bodyWeightKg: number
  country: string
  club: string | null
  coach: string | null
  events: Array<{
    event: 'LC' | 'JERK' | 'SNATCH' | 'BIATHLON'
    bellWeight: string
    duration: number
  }>
  isJudging?: number
}): Promise<{ registrantId: string } | { error: string }>
```

From lib/schema.ts (Competition type fields relevant to Add Entry):
```typescript
allowedDurations: text  // 'both' | '10' | '5'
allowedBellWeights: text // JSON string e.g. '["2x8","2x12","1x16"]'
```

From DashboardClient.tsx (dashboardData shape):
```typescript
dashboardData: {
  competition: Competition
  registrants: RegistrantWithEntries[]
  totalCount: number
} | null
```

TimetableGrid existing patterns:
- `handleInsertBlock(afterIndex)` — splices new block, renumbers, recalculates times+conflicts
- `recalculate(blocks, minRestBlocks, judgeCandidates)` — re-runs assignJudges + detectConflicts
- `isDirty` state flag set after any mutation (triggers "Unsaved changes" indicator)

EditRegistrantDialog pattern (modal conventions):
- Self-contained: `open={true}`, `onOpenChange` delegates close to parent via callback
- Parent holds state: `useState<X | null>(null)`, renders dialog when non-null
- Uses `inputClass` and `labelClass` strings for consistent styling
- Select components from '@/components/ui/select'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add delete-block to TimetableGrid</name>
  <files>app/organizerdb/queue/_components/TimetableGrid.tsx</files>
  <action>
Add a `handleDeleteBlock(blockIdx: number)` function to TimetableGrid that:
1. Clones `timeBlocks` via `structuredClone`
2. Splices out the block at `blockIdx`
3. Renumbers remaining blocks sequentially (blockNumber = i + 1)
4. Recalculates times: first block keeps its original startTime/endTime; subsequent blocks use `prev.endTime + transitionDuration` for startTime and `startTime + blockDuration` for endTime (same pattern as `handleInsertBlock`)
5. Calls `recalculate()` to re-run assignJudges + detectConflicts
6. Sets `isDirty(true)`

Add a delete button to each block row. Place it in the block number cell (`<td>` that shows `block.blockNumber`). Use a small Trash2 icon from lucide-react (already imported in other files). The button should be:
- Visually subtle: `text-raw-steel hover:text-red-400 transition-colors` (same hover pattern as RemoveRegistrantDialog trigger)
- Only visible on hover of the row or always visible but small — use `opacity-60 hover:opacity-100` pattern
- Hidden in print (add `print:hidden` class)
- Placed after the block number text: `{block.blockNumber} <button>...</button>`

Import `Trash2` from `lucide-react` at top of file.

Do NOT allow deletion if there is only 1 block remaining (disable or hide the button when `timeBlocks.length <= 1`).
  </action>
  <verify>
    <automated>cd /Users/clarence/kova-app && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Each block row in the timetable has a delete button that removes the block, renumbers remaining blocks, recalculates times and conflicts, and marks the queue as dirty (unsaved). Button is hidden when only 1 block remains.</done>
</task>

<task type="auto">
  <name>Task 2: Add Entry button and AddRegistrantDialog on organizer dashboard</name>
  <files>
    app/organizerdb/_components/AddRegistrantDialog.tsx
    app/organizerdb/_components/DashboardClient.tsx
    app/organizerdb/_components/RegistrationsTable.tsx
  </files>
  <action>
**Create `AddRegistrantDialog.tsx`** — a new dialog component following the EditRegistrantDialog pattern:

Props:
```typescript
interface AddRegistrantDialogProps {
  competitionId: string
  allowedDurations: 'both' | '10' | '5'
  allowedBellWeights: string // JSON string from competition
  onAdded: () => void
}
```

The dialog should:
1. Be self-contained with `open` state managed internally (uses DialogTrigger pattern like CSVImportModal, NOT the controlled open=true pattern of EditRegistrantDialog). The trigger button is a `UserPlus` icon button with text "Add Entry" styled like the CSV Import/Template buttons: `inline-flex items-center gap-1.5 rounded-xl border border-raw-steel/30 px-3 py-1.5 text-xs text-raw-steel transition-colors hover:border-parchment/50 hover:text-parchment`. Import `UserPlus` from lucide-react.
2. Collect these fields in a 2-column grid (same layout as EditRegistrantDialog):
   - Last Name (text, required)
   - First Name (text, required)
   - Gender (Select: Male/Female, required)
   - Body Weight kg (number, required)
   - Country (text input — simpler than the searchable dropdown in public form; organizer can type freely)
   - Judging Status (Select: Not judging / Judge only / Competing + judging)
   - Club (text, optional)
   - Coach (text, optional)
3. Below the grid, an "Events" section. Show 3 checkboxes (LC, Jerk, Snatch). When checked, reveal:
   - Bell Weight: a Select dropdown. Parse `allowedBellWeights` JSON. For LC/JERK show items starting with "2x" (double bell). For SNATCH show items starting with "1x" (single bell). Same filtering logic as in RegistrationForm.
   - Duration: if `allowedDurations === 'both'`, show a Select with 10 min / 5 min options. If `'10'` show static "10 min". If `'5'` show static "5 min".
   - When judge-only (isJudging === '1'), hide the events section entirely (judge-only registrants have no events).
4. On submit: call `registerAthlete` from `@/lib/actions/registrations` with the collected data. Handle error/success the same way as EditRegistrantDialog (show error text, call `onAdded()` on success).
5. Use `inputClass` and `labelClass` constants matching EditRegistrantDialog styling.
6. After successful add, close the dialog and call `onAdded()`.

Use `'use client'` directive. Import Dialog/DialogContent/DialogHeader/DialogTitle/DialogTrigger/DialogFooter from '@/components/ui/dialog', Button from '@/components/ui/button', Select components from '@/components/ui/select', Checkbox from '@/components/ui/checkbox'.

**Update `DashboardClient.tsx`:**
1. Import `AddRegistrantDialog` from './AddRegistrantDialog'
2. In the action bar (the `flex flex-wrap items-start gap-3` div), add `<AddRegistrantDialog>` after the CSV Template button and before GenerateQueueModal. Pass props:
   - `competitionId={dashboardData.competition.id}`
   - `allowedDurations={dashboardData.competition.allowedDurations}`
   - `allowedBellWeights={dashboardData.competition.allowedBellWeights}`
   - `onAdded={() => window.location.reload()}` (same reload pattern as CSVImportModal's onImported)

**No changes to RegistrationsTable.tsx** — the Add Entry button lives in the action bar of DashboardClient, not inside the table.
  </action>
  <verify>
    <automated>cd /Users/clarence/kova-app && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>An "Add Entry" button appears in the dashboard action bar. Clicking it opens a dialog where organizers can fill in athlete details and events. Submitting creates a registrant with serials via the registerAthlete server action. The page reloads to show the new entry in the table.</done>
</task>

</tasks>

<verification>
1. Build passes: `npx next build` completes without errors
2. Navigate to /organizerdb/queue?compId=...&startTime=540 — each block row shows a delete button; clicking it removes the block and renumbers remaining blocks
3. Navigate to /organizerdb?compId=... — "Add Entry" button visible in action bar; clicking opens dialog; filling form and submitting creates new registrant visible in table after reload
</verification>

<success_criteria>
- Delete block: blocks can be removed from queue, remaining blocks renumber correctly, times recalculate, conflicts re-evaluate, dirty flag set
- Add Entry: organizer can add a registrant with events directly from dashboard without leaving the page; registrant and entries persisted to DB with serials
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/quick/260405-0uq-delete-block-in-queue-manager-and-add-en/260405-0uq-SUMMARY.md`
</output>
