'use client'

export function CSVImportModal({
  competitionId,
  serialPrefix,
  onImported,
}: {
  competitionId: string
  serialPrefix: string
  onImported: () => void
}) {
  return null // Stub — Plan 03 implements
}
