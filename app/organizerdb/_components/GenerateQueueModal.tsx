'use client'

export function GenerateQueueModal({
  compId,
  registrantCount,
  numPlatforms,
}: {
  compId: string
  registrantCount: number
  numPlatforms: number
}) {
  return null // Stub — Plan 03 implements
}
